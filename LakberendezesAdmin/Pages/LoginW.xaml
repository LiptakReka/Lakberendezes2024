<Window x:Class="LakberendezesAdmin.Pages.LoginW"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LakberendezesAdmin.Pages"
        mc:Ignorable="d"
         Title="Bejelentkezés" Height="400" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f0f0f0">
    <Window.Resources>
        <Style TargetType="Button" x:Key="LoginButtonStyle">
            <Setter Property="Background" Value="#754F5B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                        
                            <Trigger Property="IsPressed" Value="True">



                                <Setter Property="Background" Value="#D3D5D7" />
                                <Setter Property="Foreground" Value="Black"/>

                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="#F9E0D9" CornerRadius="10" Padding="30" Margin="20,0,10,20" BorderBrush="#E6DBD0" BorderThickness="1">
            <StackPanel VerticalAlignment="Center" Height="277">

                <TextBlock Text="Bejelentkezés" 
                           FontSize="26" 
                           FontWeight="Bold" 
                           Foreground="#333"
                           HorizontalAlignment="Center" 
                           Margin="0,20,0,20"/>

           
            <TextBlock Text="Email:"
                           FontSize="14"
                           Foreground="#222"
                           Margin="0,5" HorizontalAlignment="Center"/>
            <TextBox x:Name="EmailTextBox"
                         Height="30"
                         Width="250"
                         Margin="0,5,0,0"
                         Padding="5"
                         BorderBrush="#ccc"
                         Background="#FFF"/>

            
            <TextBlock Text="Jelszó:"
                           FontSize="14"
                           Foreground="#222"
                           Margin="0,5"
                           HorizontalAlignment="Center"/>
            <PasswordBox x:Name="PasswordBox"
                             Height="30"
                             Width="250"
                             Margin="0,5,0,0"
                             Padding="5"
                             BorderBrush="#ccc"
                             Background="#FFF"/>

            
            <Button Content="Bejelentkezés"
                        Click="Login_Click"
                        Style="{StaticResource LoginButtonStyle}"
                        Margin="0,10,0,-10"/>

            
            <TextBlock x:Name="ErrorMessage"
                           Text=""
                           Foreground="Red"
                           FontSize="12"
                           Visibility="Collapsed"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
