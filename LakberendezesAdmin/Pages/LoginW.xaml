<Window x:Class="LakberendezesAdmin.Pages.LoginW"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LakberendezesAdmin.Pages"
        mc:Ignorable="d"
         Title="Bejelentkezés" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#f0f0f0">
    <Window.Resources>
        <Style TargetType="Button" x:Key="LoginButtonStyle">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                Padding="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>

                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White" CornerRadius="10" Padding="30" Margin="20,0,10,20" BorderBrush="#D0D0D0" BorderThickness="1">
            <StackPanel VerticalAlignment="Center">

                <TextBlock Text="Bejelentkezés" 
                           FontSize="26" 
                           FontWeight="Bold" 
                           Foreground="#333"
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,20"/>

           
            <TextBlock Text="Email:"
                           FontSize="14"
                           Foreground="#555"
                           Margin="0,5"/>
            <TextBox x:Name="EmailTextBox"
                         Height="30"
                         Width="250"
                         Margin="0,5,0,0"
                         Padding="5"
                         BorderBrush="#ccc"
                         Background="#FFF"/>

            
            <TextBlock Text="Jelszó:"
                           FontSize="14"
                           Foreground="#555"
                           Margin="0,5"/>
            <PasswordBox x:Name="PasswordBox"
                             Height="30"
                             Width="250"
                             Margin="0,5,0,0"
                             Padding="5"
                             BorderBrush="#ccc"
                             Background="#FFF"/>

            
            <Button Content="Bejelentkezés"
                        Click="Login_Click"
                        Style="{StaticResource LoginButtonStyle}"
                        Margin="0,10,0,-10"/>

            
            <TextBlock x:Name="ErrorMessage"
                           Text=""
                           Foreground="Red"
                           FontSize="12"
                           Visibility="Collapsed"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
